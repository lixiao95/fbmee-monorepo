packages:
  # all packages in direct subdirs of packages/
  - 'packages/*'
  # all packages in subdirs of components/
  - 'components/**'
  # all packages in subdirs of requestApi
  - 'apis/**'
  - # all packages in subdirs of utils
  - 'utils/**'
